namespace ExcelBrowser.Model {

    public enum ChangeType {
        Unknown,
        Add,
        Remove,
        SetActive,
        SetVisible,
        SetWindowState,
        SetSheetIndex,
        SetSheetTabColor,
        SetWindowVisibleSheet,
        SessionStart
    }
}
